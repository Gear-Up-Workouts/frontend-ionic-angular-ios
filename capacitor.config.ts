import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'gear-up.app.workout-tracker',
  appName: 'gear-up',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
